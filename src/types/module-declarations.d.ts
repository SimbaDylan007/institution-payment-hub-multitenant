// This file is to declare modules that don't have their own TypeScript type definitions.
declare module '@tailwindcss/line-clamp' {
    const plugin: any; // Using 'any' as a fallback, as specific types aren't available
    export default plugin;
}